{
  "name": "echoscribe-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "turbo run dev --filter=frontend",
    "dev:gateway": "turbo run dev --filter=gateway"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
